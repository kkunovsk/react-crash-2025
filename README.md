# react-crash-2025
This repo holds a job posting site built from TypeScript and React that allows you to create, edit, and delete job postings.

To run the server:<br>
`npm run server`

To run the client:<br>
`npm run dev`

Open http://localhost:3002/ in a browser (or a similar link with a different port) provided by running `npm run dev`.
